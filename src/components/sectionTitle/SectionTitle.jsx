const SectionTitle = ({heading}) => {
    return (
        <div>
            <h3 className="text-xl md:text-2xl uppercase py-5 font-bold text-center">{heading}</h3>
        </div>
    );
};

export default SectionTitle;